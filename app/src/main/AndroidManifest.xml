<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lab11_o22">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lab11_o22">
        <activity
            android:name=".ScoreFinal"
            android:label="@string/title_activity_score_final"
            android:theme="@style/Theme.Lab11_o22.NoActionBar"></activity>
        <activity android:name=".Quiz2" />
        <activity android:name=".Quiz1" />
        <activity android:name=".Register" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>